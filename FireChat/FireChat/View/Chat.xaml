<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FireChat.View.Chat">
    <ContentPage.Content>
        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="4*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <ListView Grid.Row="0" ItemsSource="{Binding .}" x:Name="_lstChat"
				SeparatorVisibility="None" Margin="2">
                <ListView.ItemTemplate>
                    <DataTemplate>

                        <TextCell Text="{Binding UserName}" Detail="{Binding UserMessage}"/>

                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            
            <Entry Grid.Row="1" x:Name="_etMessage"/>

            <Grid Grid.Row="2" RowSpacing="5">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="3" Text="Enviar" />
            </Grid>
            
            
        </Grid>
    </ContentPage.Content>
</ContentPage>